# Monteiro - simple builder utility
Experiment with the provided montefile:
`monteiro --help`
`monteiro --clean --montefile-path ./montefile.toml`
`monteiro --montefile < montefile.toml`
`monteiro --clean --server && source test.sh`
